deptrac:
  paths:
    - ./src

  layers:
    - name: Domain
      collectors:
        - type: classLike
          value: ^WordFrequencyCounter\\(Cli|CliOptions|Entry|Result|WordCounter)$

  ruleset:
    Domain: []
